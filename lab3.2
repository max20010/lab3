
#include "pch.h"
#include <iostream>
using namespace std;
void swipe(int x, int y) {
	x = y;
	cout << x << endl;
}
int main()
{
	int a, b,c;
	cin >> a;
	cin >> b;
	cin >> c;
	swipe(a, b);
	swipe(b, c);
	swipe(c, a);
}

